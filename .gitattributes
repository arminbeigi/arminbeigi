# فایل‌های batch ویندوز باید همیشه خط‌پایان CRLF داشته باشند
*.bat text eol=crlf
*.cmd text eol=crlf
# اسکریپت Inno Setup هم روی ویندوز کامپایل می‌شود
*.iss text eol=crlf
# اسکریپت PowerShell — CRLF (و BOM به‌صورت محتوا حفظ می‌شود)
*.ps1 text eol=crlf
