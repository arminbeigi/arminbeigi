# وابستگی‌های سیستم ارسال خودکار پیش فاکتور
watchdog>=4.0.0         # نظارت بر تغییرات پوشه (نسخه‌ی خط فرمان)
requests>=2.31.0        # درخواست‌های HTTP
PyYAML>=6.0             # خواندن فایل تنظیمات (نسخه‌ی خط فرمان)
kavenegar>=1.1.0        # کاوه‌نگار
rubpy>=6.6.0            # روبیکا (اختیاری)

# ── رابط گرافیکی (نسخه‌ی ویندوز) ──
customtkinter>=5.2.0    # محیط گرافیکی مدرن
cryptography>=42.0.0    # ذخیره‌ی رمزنگاری‌شده‌ی اطلاعات ورود

# ── ساخت فایل اجرایی ویندوز (فقط هنگام build) ──
# pyinstaller>=6.0.0
